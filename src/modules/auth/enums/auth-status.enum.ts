export enum Status {
  OK = 'ok',
  USER_NAME_EXISTS = 'user_name_exists',
  NEW_PASSWORD_CREATED = 'new_password_created',
  INVALID_CREDENTIALS = 'invalid_credentials',
  ATTEMPTS_EXCEDDED = 'attempts_exceeded',
}
