export enum FieldType {
    NUMBER = 'number',
    INTEGER = 'integer',
    DECIMAL = 'decimal',
    STRING = 'string',
    DATE = 'date',
    DATETIME = 'datetime',
    DATE_RANGE = 'date_range',
    NOW_DATE_RANGE = 'now_date_range'
}