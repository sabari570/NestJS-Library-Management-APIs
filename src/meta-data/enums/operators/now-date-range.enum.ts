import { Operators } from './operators.enum';

export enum NowDateRangeOperators {
    IS_LESS = Operators.IS_LESS,
    IS_MORE = Operators.IS_MORE,
    IS_AFTER_NEXT = Operators.IS_AFTER_NEXT,
    IS_BEFORE_LAST = Operators.IS_BEFORE_LAST,
}